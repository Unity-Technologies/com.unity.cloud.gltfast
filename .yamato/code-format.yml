#######################################################
###                                                 ###
###  Auto-generated by Recipe Engine, DO NOT EDIT.  ###
###                                                 ###
#######################################################
# Source: Gltfast.Cookbook.Recipes.CodeFormatRecipe
# Code formatting
code_format:
  name: Code Format
  dependencies:
  - path: .yamato/code-format.yml#code_format_gltfast-test
  - path: .yamato/code-format.yml#code_format_gltfast-test-builtin
  - path: .yamato/code-format.yml#code_format_gltfast-test-entities
  - path: .yamato/code-format.yml#code_format_gltfast-test-hdrp
  - path: .yamato/code-format.yml#code_format_gltfast-test-minimalistic
code_format_gltfast-test:
  name: Code Format glTFast-Test
  agent:
    image: package-ci/ubuntu-22.04:v4
    type: Unity::VM
    flavor: b1.large
  commands:
  - command: unity-downloader-cli --unity-version 6000.0 --path Editor --wait -c editor -c il2cpp
    timeout: 10
    retries: 3
  - command: Editor/Unity -projectPath Projects/glTFast-Test -executeMethod Packages.Rider.Editor.RiderScriptEditor.SyncSolution -upmNoDefaultPackages -batchmode -nographics -quit -logfile UnityProjectSetup.log
  - name: Certify Git state
    command: |-
      # Ignore changes to ProjectVersion.txt and manifest.json
      git checkout Projects/glTFast-Test/ProjectSettings/ProjectVersion.txt Projects/glTFast-Test/Packages/manifest.json Packages/com.unity.cloud.gltfast.tests/Tests/Runtime/Export/Materials/**/*.mat
      if ! git diff --quiet; then
        git status --porcelain
        MESSAGE="Git working directory is not clean after generating solution file and prior to formatting. Please ensure that the project is in a clean state before running the format job."
        echo $MESSAGE
        curl -X POST -d "{\"title\": \"Unclean State\",\"conclusion\": \"failure\",\"summary\": \"$MESSAGE\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  - name: .NET Format
    command: dotnet format Projects/glTFast-Test/glTFast-Test.sln
  - name: Detect formatting changes
    command: |-
      if git diff --quiet; then
        echo "No formatting changes detected required."
        curl -X POST -d "{\"title\": \"Code Format Valid\",\"conclusion\": \"success\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
      else
        git diff > format.patch
        echo '{"title":"Code Format Invalid","conclusion":"failure","tags":["code-format","slack"],"resultType":"userFriendly","summary":"Some files have incorrect formatting! The `code_format_patch` artifact contains a patch that fixes it.","data":' > result.json
        cat format.patch | jq -Rs . >> result.json
        echo '}' >> result.json
        curl -X POST -d @result.json -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  artifacts:
    code_format_patch:
      paths:
      - format.patch
code_format_gltfast-test-builtin:
  name: Code Format glTFast-Test-BuiltIn
  agent:
    image: package-ci/ubuntu-22.04:v4
    type: Unity::VM
    flavor: b1.large
  commands:
  - command: unity-downloader-cli --unity-version 6000.0 --path Editor --wait -c editor -c il2cpp
    timeout: 10
    retries: 3
  - command: Editor/Unity -projectPath Projects/glTFast-Test-BuiltIn -executeMethod Packages.Rider.Editor.RiderScriptEditor.SyncSolution -upmNoDefaultPackages -batchmode -nographics -quit -logfile UnityProjectSetup.log
  - name: Certify Git state
    command: |-
      # Ignore changes to ProjectVersion.txt and manifest.json
      git checkout Projects/glTFast-Test-BuiltIn/ProjectSettings/ProjectVersion.txt Projects/glTFast-Test-BuiltIn/Packages/manifest.json Packages/com.unity.cloud.gltfast.tests/Tests/Runtime/Export/Materials/**/*.mat
      if ! git diff --quiet; then
        git status --porcelain
        MESSAGE="Git working directory is not clean after generating solution file and prior to formatting. Please ensure that the project is in a clean state before running the format job."
        echo $MESSAGE
        curl -X POST -d "{\"title\": \"Unclean State\",\"conclusion\": \"failure\",\"summary\": \"$MESSAGE\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  - name: .NET Format
    command: dotnet format Projects/glTFast-Test-BuiltIn/glTFast-Test-BuiltIn.sln
  - name: Detect formatting changes
    command: |-
      if git diff --quiet; then
        echo "No formatting changes detected required."
        curl -X POST -d "{\"title\": \"Code Format Valid\",\"conclusion\": \"success\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
      else
        git diff > format.patch
        echo '{"title":"Code Format Invalid","conclusion":"failure","tags":["code-format","slack"],"resultType":"userFriendly","summary":"Some files have incorrect formatting! The `code_format_patch` artifact contains a patch that fixes it.","data":' > result.json
        cat format.patch | jq -Rs . >> result.json
        echo '}' >> result.json
        curl -X POST -d @result.json -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  artifacts:
    code_format_patch:
      paths:
      - format.patch
code_format_gltfast-test-entities:
  name: Code Format glTFast-Test-entities
  agent:
    image: package-ci/ubuntu-22.04:v4
    type: Unity::VM
    flavor: b1.large
  commands:
  - command: unity-downloader-cli --unity-version 6000.0 --path Editor --wait -c editor -c il2cpp
    timeout: 10
    retries: 3
  - command: Editor/Unity -projectPath Projects/glTFast-Test-entities -executeMethod Packages.Rider.Editor.RiderScriptEditor.SyncSolution -upmNoDefaultPackages -batchmode -nographics -quit -logfile UnityProjectSetup.log
  - name: Certify Git state
    command: |-
      # Ignore changes to ProjectVersion.txt and manifest.json
      git checkout Projects/glTFast-Test-entities/ProjectSettings/ProjectVersion.txt Projects/glTFast-Test-entities/Packages/manifest.json Packages/com.unity.cloud.gltfast.tests/Tests/Runtime/Export/Materials/**/*.mat
      if ! git diff --quiet; then
        git status --porcelain
        MESSAGE="Git working directory is not clean after generating solution file and prior to formatting. Please ensure that the project is in a clean state before running the format job."
        echo $MESSAGE
        curl -X POST -d "{\"title\": \"Unclean State\",\"conclusion\": \"failure\",\"summary\": \"$MESSAGE\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  - name: .NET Format
    command: dotnet format Projects/glTFast-Test-entities/glTFast-Test-entities.sln
  - name: Detect formatting changes
    command: |-
      if git diff --quiet; then
        echo "No formatting changes detected required."
        curl -X POST -d "{\"title\": \"Code Format Valid\",\"conclusion\": \"success\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
      else
        git diff > format.patch
        echo '{"title":"Code Format Invalid","conclusion":"failure","tags":["code-format","slack"],"resultType":"userFriendly","summary":"Some files have incorrect formatting! The `code_format_patch` artifact contains a patch that fixes it.","data":' > result.json
        cat format.patch | jq -Rs . >> result.json
        echo '}' >> result.json
        curl -X POST -d @result.json -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  artifacts:
    code_format_patch:
      paths:
      - format.patch
code_format_gltfast-test-hdrp:
  name: Code Format glTFast-Test-HDRP
  agent:
    image: package-ci/ubuntu-22.04:v4
    type: Unity::VM
    flavor: b1.large
  commands:
  - command: unity-downloader-cli --unity-version 6000.0 --path Editor --wait -c editor -c il2cpp
    timeout: 10
    retries: 3
  - command: Editor/Unity -projectPath Projects/glTFast-Test-HDRP -executeMethod Packages.Rider.Editor.RiderScriptEditor.SyncSolution -upmNoDefaultPackages -batchmode -nographics -quit -logfile UnityProjectSetup.log
  - name: Certify Git state
    command: |-
      # Ignore changes to ProjectVersion.txt and manifest.json
      git checkout Projects/glTFast-Test-HDRP/ProjectSettings/ProjectVersion.txt Projects/glTFast-Test-HDRP/Packages/manifest.json Packages/com.unity.cloud.gltfast.tests/Tests/Runtime/Export/Materials/**/*.mat
      if ! git diff --quiet; then
        git status --porcelain
        MESSAGE="Git working directory is not clean after generating solution file and prior to formatting. Please ensure that the project is in a clean state before running the format job."
        echo $MESSAGE
        curl -X POST -d "{\"title\": \"Unclean State\",\"conclusion\": \"failure\",\"summary\": \"$MESSAGE\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  - name: .NET Format
    command: dotnet format Projects/glTFast-Test-HDRP/glTFast-Test-HDRP.sln
  - name: Detect formatting changes
    command: |-
      if git diff --quiet; then
        echo "No formatting changes detected required."
        curl -X POST -d "{\"title\": \"Code Format Valid\",\"conclusion\": \"success\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
      else
        git diff > format.patch
        echo '{"title":"Code Format Invalid","conclusion":"failure","tags":["code-format","slack"],"resultType":"userFriendly","summary":"Some files have incorrect formatting! The `code_format_patch` artifact contains a patch that fixes it.","data":' > result.json
        cat format.patch | jq -Rs . >> result.json
        echo '}' >> result.json
        curl -X POST -d @result.json -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  artifacts:
    code_format_patch:
      paths:
      - format.patch
code_format_gltfast-test-minimalistic:
  name: Code Format glTFast-Test-minimalistic
  agent:
    image: package-ci/ubuntu-22.04:v4
    type: Unity::VM
    flavor: b1.large
  commands:
  - command: unity-downloader-cli --unity-version 6000.0 --path Editor --wait -c editor -c il2cpp
    timeout: 10
    retries: 3
  - command: Editor/Unity -projectPath Projects/glTFast-Test-minimalistic -executeMethod Packages.Rider.Editor.RiderScriptEditor.SyncSolution -upmNoDefaultPackages -batchmode -nographics -quit -logfile UnityProjectSetup.log
  - name: Certify Git state
    command: |-
      # Ignore changes to ProjectVersion.txt and manifest.json
      git checkout Projects/glTFast-Test-minimalistic/ProjectSettings/ProjectVersion.txt Projects/glTFast-Test-minimalistic/Packages/manifest.json Packages/com.unity.cloud.gltfast.tests/Tests/Runtime/Export/Materials/**/*.mat
      if ! git diff --quiet; then
        git status --porcelain
        MESSAGE="Git working directory is not clean after generating solution file and prior to formatting. Please ensure that the project is in a clean state before running the format job."
        echo $MESSAGE
        curl -X POST -d "{\"title\": \"Unclean State\",\"conclusion\": \"failure\",\"summary\": \"$MESSAGE\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  - name: .NET Format
    command: dotnet format Projects/glTFast-Test-minimalistic/glTFast-Test-minimalistic.sln
  - name: Detect formatting changes
    command: |-
      if git diff --quiet; then
        echo "No formatting changes detected required."
        curl -X POST -d "{\"title\": \"Code Format Valid\",\"conclusion\": \"success\"}" -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
      else
        git diff > format.patch
        echo '{"title":"Code Format Invalid","conclusion":"failure","tags":["code-format","slack"],"resultType":"userFriendly","summary":"Some files have incorrect formatting! The `code_format_patch` artifact contains a patch that fixes it.","data":' > result.json
        cat format.patch | jq -Rs . >> result.json
        echo '}' >> result.json
        curl -X POST -d @result.json -H 'Content-Type: application/json' $YAMATO_REPORTING_SERVER/result
        exit 1
      fi
  artifacts:
    code_format_patch:
      paths:
      - format.patch

